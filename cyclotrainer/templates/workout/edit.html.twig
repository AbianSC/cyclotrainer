{% extends 'base.html.twig' %}

{% block title %}Edit Workout{% endblock %}

{% block body %}
    <h1>Edit Workout</h1>

    {{ include('workout/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ referer ? referer : path('app_workout_index') }}">back to list</a>

    {{ include('workout/_delete_form.html.twig') }}
{% endblock %}
